connector.name=iceberg

# Hive Metastore
iceberg.catalog.type=hive_metastore
hive.metastore.uri=thrift://metastore:9083

# S3/MinIO access for data and metadata files (environment variables approach)
# The S3 credentials and endpoint will be set via environment variables

# File format default
iceberg.file-format=PARQUET
